kind: Service
apiVersion: v1
metadata:
  name: hd55-ambulance-webapi
spec:
  selector:
    pod: hd55-ambulance-webapi-label
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: webapi-port
